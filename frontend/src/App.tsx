import {useState} from "react";
import {LayoutDashboard,FolderKanban,ClipboardList,Coins,Calculator,BarChart3,History,Download,Menu} from "lucide-react";
import "./styles.css";
const nav=[["Dashboard",LayoutDashboard],["Projects",FolderKanban],["BOQ",ClipboardList],["Cost Rates",Coins],["Rate Analysis",Calculator],["Summary",BarChart3],["Estimate Versions",History],["Exports",Download]] as const;
export default function App(){const [active,setActive]=useState("Dashboard"); const [open,setOpen]=useState(true); return <div className="app"><aside className={open?"sidebar":"sidebar collapsed"}><div className="brand"><div className="brandMark">CE</div>{open&&<div><strong>Cost Estimate</strong><span>Construction QS</span></div>}</div><nav>{nav.map(([label,Icon])=><button className={active===label?"nav active":"nav"} onClick={()=>setActive(label)} key={label}><Icon size={19}/>{open&&<span>{label}</span>}</button>)}</nav><div className="sideBottom">{open&&<small>v0.1 • Ethiopia-ready</small>}</div></aside><main><header><button className="iconBtn" onClick={()=>setOpen(!open)}><Menu size={21}/></button><div><div className="eyebrow">CONSTRUCTION COST ESTIMATION</div><h1>{active}</h1></div><div className="headerRight"><span className="statusDot"/> API ready</div></header><section className="content">{active==="Dashboard"?<Dashboard/>:<Placeholder title={active}/>}</section></main></div>}
function Dashboard(){return <><div className="hero"><div><p className="eyebrow">ESTIMATING WORKSPACE</p><h2>Build accurate estimates with confidence.</h2><p className="muted">Manage projects, quantities, rates, BOQs and estimate versions in one workspace.</p></div><button className="primary">+ New Project</button></div><div className="cards"><Card title="Projects" value="0" icon={<FolderKanban/>}/><Card title="BOQ Items" value="0" icon={<ClipboardList/>}/><Card title="Cost Rates" value="0" icon={<Coins/>}/><Card title="Estimate Value" value="0 ETB" icon={<BarChart3/>}/></div><div className="grid"><div className="panel"><h3>Quick actions</h3><div className="actions"><button>+ Create project</button><button>Upload BOQ</button><button>Manage cost rates</button><button>Run rate analysis</button></div></div><div className="panel"><h3>Estimation workflow</h3><ol><li>Set up your project</li><li>Add BOQ quantities and rates</li><li>Build detailed rate analyses</li><li>Review summary and versions</li><li>Export Excel or PDF</li></ol></div></div></>}
function Card({title,value,icon}:{title:string,value:string,icon:React.ReactNode}){return <div className="card"><div className="cardIcon">{icon}</div><div><span>{title}</span><strong>{value}</strong></div></div>}
function Placeholder({title}:{title:string}){return <div className="empty panel"><div className="emptyIcon">◈</div><h2>{title}</h2><p className="muted">This workspace is ready. The next step is connecting it to the live estimating API.</p><button className="primary">Coming next</button></div>}